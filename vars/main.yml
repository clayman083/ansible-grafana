---

grafana_dir: "{{ services_dir }}/grafana"
grafana_dashboards_dir: "{{ grafana_dir }}/dashboards"

# Container settings
grafana_container:
  image: grafana/grafana:6.0.2
  name: grafana
  dns: "{{ private_network.address }}"
  network: "{{ grafana_network.name }}"
  restart: always
