{
  "services": [
    {
      "name": "{{ grafana.service.name }}",
{% if grafana.service.tags %}
      "tags": ["{{ grafana.service.tags|join('", "') }}"],
{% endif %}
{% if grafana.service.meta %}
      "meta": {
{% for key, value in grafana.service.meta.items() %}
        "{{ key }}": "{{ value }}"{% if not loop.last %},{% endif %}

{% endfor %}
      },
{% endif %}
      "address": "{{ grafana.host }}",
      "port": {{ grafana.port }}
    }
  ]
}
